---
    const prodHost = 'thehistoryofseo.com';
    const requestUrl = new URL(Astro.request.url);
    const currentHost = requestUrl.host;
---

{currentHost !== prodHost && (
    <meta name="robots" content="noindex,nofollow" />
)}